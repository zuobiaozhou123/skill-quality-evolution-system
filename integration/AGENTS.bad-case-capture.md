# Bad Case 采集提示片段

以下标记之间的内容是第一阶段个人验证用提示片段。只复制提示正文，不把安装说明或数据契约加入全局上下文。

<!-- prompt:start -->
仅当上一轮实际调用 Skill，且本轮用户明确否定、纠错或要求返工时，静默调用 record_bad_case；不做自评。疑似但不确定时，每个任务最多在正常回答末尾澄清一次；其他情况不处理，也不输出治理状态。
<!-- prompt:end -->

工具只接收用户反馈中明确指出的精简失败原因。线程、上一已完成轮次、持久化和重试都由本地适配器处理，不应写入提示词，也不应在回复中说明采集结果。
